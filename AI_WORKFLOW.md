# AI 工作流程與不可跳過的 UI 驗收門檻

## 目的

本檔案係本專案所有後續 AI／開發修改的持續規則。它不是某一次問題的備註，而是每次修改都必須遵守的交付條件。

## 任何 UI 修改的固定流程

每次修改提詞機、查單字、手寫板、輸入框、滾動、手機版面或按鈕後，必須按以下順序完成：

1. **先讀取本檔案及 `修改記錄_*.md`**，了解既有失敗經驗和使用者要求。
2. **先建立測試場景**：使用手機比例／手機 viewport，不可以只用寬版桌面畫面。
3. **實際操作修改涉及的流程**：至少按一次相關按鈕，輸入一次，並走完整個狀態轉換。例如手寫板要開啟、落筆、辨認、揀候選字及恢復版面。
4. **實際查看畫面**：檢查文字重疊、文字飄位、工具列走位、上下跳動、裁切、按鈕消失和輸入內容順序。DOM、CSS、語法檢查不能代替畫面檢查。
5. **發現任何畫面問題就修正並重新由第 2 步測試**，不能只記錄問題或直接交付。
6. **產生驗收證據**：在修改記錄中寫明 viewport、操作步驟、實際觀察結果及截圖／瀏覽器檢查結果。
7. **未完成手機實測前，禁止使用「完成」「已修好」「已驗收」等結論**。只能說「已修改，未驗收」。

## 強制交付格式

每次交付前，回覆內必須列出：

- 修改的 commit；
- 實測 viewport 或裝置尺寸；
- 實際操作步驟；
- 由畫面觀察到的結果；
- 若有任何項目未測，明確標示「未驗收」，不能用部署成功代替測試成功。

## 本專案特別永久規則

- 不得只做程式碼／DOM／`node --check` 檢查後就交付 UI 修改。
- 不得用寬版桌面截圖代表手機版驗收。
- 不得把使用者已講過的問題重新覆述當成答案；要提供新證據、修改或明確說明尚未完成。
- 每次修改後必須更新相應的 `修改記錄_*.md`，寫明今次實測，而不只是寫修改原因。
