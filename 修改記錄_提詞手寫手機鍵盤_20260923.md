# 提詞機手寫模式手機鍵盤修正記錄

日期：2026-09-23

## 問題

手機使用提詞機手寫板時，辨認字元並插入提詞內容後，原生手機輸入法會重新彈出，導致畫面被鍵盤上下推動及滾動，影響使用。

## 原因

`insertHandwritingCharacter()` 在辨認成功後重新 render，並再次呼叫 `inputTextArea.focus()`。手機因此把 textarea 當成一般文字輸入框，重新叫出系統輸入法。

## 修正

- 手寫板開啟期間，提詞 textarea 加上 `readonly`。
- 手寫板開啟期間，設定 `inputmode="none"`。
- 開啟手寫板時先對 textarea 執行 `blur()`，收起已經開啟嘅手機鍵盤。
- 手寫辨認及插字後不再呼叫 `focus()`，只保留游標位置資料。
- 關閉手寫板後，textarea 恢復 `inputmode="text"`，手機預設輸入法先可以正常使用。

## 期望行為

手寫模式期間只可以用手寫板，不會彈出手機原生鍵盤；關閉手寫模式後，提詞輸入框恢復一般手機文字輸入功能。

## 第二輪修正：手寫板上下佈局（2026-09-23）

用家進一步指出，即使鍵盤不再彈出，辨認後新增文字會令上方提詞輸入框佔用太多高度，手機仍然需要上下拉動。

已加入手寫板開啟期間的一行模式：

- 上方提詞輸入框縮成只顯示一行高度。
- 文字不換行，超出部分自動保持在最右側可見位置。
- 下方獨立手寫板保留完整操作高度。
- 手寫插入新字後自動把上方一行移到最新字元位置。
- 關閉手寫板後，輸入框恢復原本完整高度及可換行輸入。

目標係讓使用者開啟手寫板後可以專心喺下面寫字，唔需要再為咗睇上下內容而滾動畫面。

## 第三輪修正：自動推到手寫板位置（2026-09-23）

用家指出，手寫辨認後下方會出現四至五個候選字，但上方功能列及輸入區仍佔住畫面，候選字未能完整顯示，使用者需要再手動上下拉動。

已加入 `scrollToHandwritingPanel()`：開啟手寫板、辨認完成顯示候選字，以及選字插入內容後，會自動將手寫板頂部對齊內容區頂部，令上方功能列褪出畫面，只集中顯示手寫板及候選字。關閉手寫板後，原本頁面位置及完整輸入區會恢復。
