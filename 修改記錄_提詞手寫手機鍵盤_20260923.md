# 提詞機手寫模式手機鍵盤修正記錄

日期：2026-09-23

## 問題

手機使用提詞機手寫板時，辨認字元並插入提詞內容後，原生手機輸入法會重新彈出，導致畫面被鍵盤上下推動及滾動，影響使用。

## 原因

`insertHandwritingCharacter()` 在辨認成功後重新 render，並再次呼叫 `inputTextArea.focus()`。手機因此把 textarea 當成一般文字輸入框，重新叫出系統輸入法。

## 修正

- 手寫板開啟期間，提詞 textarea 加上 `readonly`。
- 手寫板開啟期間，設定 `inputmode="none"`。
- 開啟手寫板時先對 textarea 執行 `blur()`，收起已經開啟嘅手機鍵盤。
- 手寫辨認及插字後不再呼叫 `focus()`，只保留游標位置資料。
- 關閉手寫板後，textarea 恢復 `inputmode="text"`，手機預設輸入法先可以正常使用。

## 期望行為

手寫模式期間只可以用手寫板，不會彈出手機原生鍵盤；關閉手寫模式後，提詞輸入框恢復一般手機文字輸入功能。

## 第二輪修正：手寫板上下佈局（2026-09-23）

用家進一步指出，即使鍵盤不再彈出，辨認後新增文字會令上方提詞輸入框佔用太多高度，手機仍然需要上下拉動。

已加入手寫板開啟期間的一行模式：

- 上方提詞輸入框縮成只顯示一行高度。
- 文字不換行，超出部分自動保持在最右側可見位置。
- 下方獨立手寫板保留完整操作高度。
- 手寫插入新字後自動把上方一行移到最新字元位置。
- 關閉手寫板後，輸入框恢復原本完整高度及可換行輸入。

目標係讓使用者開啟手寫板後可以專心喺下面寫字，唔需要再為咗睇上下內容而滾動畫面。

## 第三輪修正：自動推到手寫板位置（2026-09-23）

用家指出，手寫辨認後下方會出現四至五個候選字，但上方功能列及輸入區仍佔住畫面，候選字未能完整顯示，使用者需要再手動上下拉動。

已加入 `scrollToHandwritingPanel()`：開啟手寫板、辨認完成顯示候選字，以及選字插入內容後，會自動將手寫板頂部對齊內容區頂部，令上方功能列褪出畫面，只集中顯示手寫板及候選字。關閉手寫板後，原本頁面位置及完整輸入區會恢復。

## 第四輪修正：實測後改用內容區 scrollTop（2026-09-23）

今次用瀏覽器實際開啟公開網站，並將內容區模擬成手機可滾動高度測試。測試確認單靠 `scrollIntoView()` 未必會滾動真正負責內容的 `.content` 容器，可能只滾動 body，因此功能列仍然會留在畫面內。

已改為直接計算手寫板相對 `.content` 的位置並設定 `.content.scrollTop`，再於短暫延遲後重做一次定位。目標係確保開啟手寫板、候選字出現及插字後，功能列會褪走，畫面頂部對齊手寫板。

## 第五輪修正：手寫輸入方向倒轉（2026-09-23）

用家實測發現連續寫「今天我們」會變成「們我天今」。原因係每次選擇候選字後頁面會重新 render，textarea 被重建，原生 `selectionStart` 會回到 0；下一個字因此不斷插到最左邊。

已改為手寫模式使用獨立的 `state.handwritingCursor`。開啟手寫板時由文章尾部開始，之後每次插字都使用上一個字更新後的 cursor，不再依賴重新 render 後 textarea 的 selection。預期輸入順序會恢復為「今 → 天 → 我 → 們」。

## 第六輪修正：確認後收窄手寫區，不再上下滾動（2026-09-23）

用家指出，辨認後若繼續用頁面上下滾動去顯示候選字，畫面會騰上騰落，操作混亂。已改為辨認成功後將手寫畫布及辨認按鈕收窄／摺起，只保留標題、狀態及候選字按鈕，讓候選字直接出現在原位的空間。

候選字選完後，手寫區恢復完整高度，方便繼續寫下一個字；亦提供「再寫一個字」按鈕。辨認及選字後不再呼叫頁面滾動，避免畫面跳動。

## 第七輪修正：半高手寫介面（2026-09-23）

上一版辨認後錯誤地完全收起畫布及操作按鈕，畫面只剩候選字，使用者會以為手寫輸入欄消失。現已改回保留完整手寫介面：辨認後只將畫布高度縮至約 200px（約原本一半），清除及辨認按鈕繼續顯示，候選字放在其下方。

同時保留辨認前的內容區 scrollTop，縮細時不再令上方一行輸入欄向下移或觸發頁面滾動。選字後手寫區恢復原本高度。

## 第八輪修正：上方固定一行、下方半高手寫板（2026-09-23）

用家進一步明確指定兩個互相獨立的區域規則：

1. 上方提詞輸入欄開啟手寫模式後縮成一行，並固定在內容區頂部；上方原本的朗讀、清除、儲存、教學及全螢幕工具列暫時隱藏，避免工具列重新出現或把輸入欄推落。
2. 下方手寫板在辨認確認後只縮細約一半高度，仍然保留手寫標題、畫布、清除及辨認按鈕；多出的空間用來顯示候選字。
3. 選擇候選字後，下方手寫板恢復原本尺寸。
4. 整個流程不依靠內容區上下滾動；辨認及選字時保留原本 `scrollTop`，開啟手寫模式時將內容區定位在頂部。

今次修正係針對上一版錯誤：上一版只收起手寫板，令使用者看不到仍然需要使用的手寫介面；同時沒有真正固定上方輸入欄。

## 第九輪修正：手寫板由底部向上縮半（2026-09-23）

用家再次明確指出縮細方向：辨認確認後，手寫板的上邊／頂線必須保持原位，不能向下移；只可以由底部向上收，保留上半截手寫板。候選字放在收窄後的下方空間。

已改為在辨認開始前讀取手寫畫布實際高度，辨認成功後將畫布高度設定為原高度的一半，並以手寫板頂部為固定基準。畫布容器保持 `overflow:hidden`，因此縮細只會裁走底部，不會移動頂部。選字、清除重寫或再寫時會清除半高狀態，恢復原本尺寸。

## 第十輪修正：識別中文標點符號（2026-09-23）

用家發現手寫中文標點後完全沒有候選結果。原因係舊過濾器只接受 Unicode Han 漢字，Google 手寫辨認服務即使回傳「，。！？：；、」等標點，程式都會將它們丟走。

已加入中文標點及全形符號白名單，包括逗號、句號、問號、感嘆號、冒號、分號、頓號、括號、引號、書名號、省略號、破折號等；同時保留常見半形標點，令標點候選字可以正常顯示及插入提詞內容。
